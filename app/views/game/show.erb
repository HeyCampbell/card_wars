
<% player1 = Player.find(session[:player1]) %>
<% player2 = Player.find(session[:player2]) %>

<div id="game_container">
  <div id="left_score">
    <h2><%= player1.name %></h2>
    <h3>Score: <%= player1.cards.count %> </h3>
  </div>

  <div id="left_card">
    <%= player1.cards.first.name %>
  </div>

  <div id="right_card">
     <%= player2.cards.first.name %>
  </div>

  <div id="right_score">
    <h2><%= player2.name %></h2>
    <h3>Score: <%= player2.cards.count %> </h3>
  </div>

  <div id="flip_button">
    <form action="/game" method="POST">

    <button type="submit" name="Battle!">Battle!</button>
    </form>
  </div>

  <form action="/signout" method="POST">
    <button type="submit" name="Signout">Signout</button>
  </form>

</div>


